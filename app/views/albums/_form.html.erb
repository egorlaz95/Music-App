<%= form_with(model: album) do |form| %>
  <% if album.errors.any? %>
    <div style="color: red" class>
      <h2><%= pluralize(album.errors.count, "error") %> prohibited this album from being saved:</h2>

      <ul>
        <% album.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="wrapper">
    <form action="#">
          <div class="h5 font-weight-bold mb-3"><%= form.label :artist%></div>
          <div class="form-group d-flex align-items-center">
            <%= form.text_field :artist, class: "form-control" %>
          </div>
          <div class="h5 font-weight-bold mb-3"><%= form.label :title %></div>
        <div class="form-group d-flex align-items-center">
          <%= form.text_field :title, class: "form-control" %>
        </div>         
          <%= form.submit (t "create_album"), class: "btn btn-primary mb-3"%>                    
    </form>
  </div>

<% end %>