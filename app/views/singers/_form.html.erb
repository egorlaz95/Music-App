<%= form_with(model: singer) do |form| %>
  <% if singer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(singer.errors.count, "error") %> prohibited this singer from being saved:</h2>

      <ul>
        <% singer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="wrapper">
    <form action="#">
        <div class="h5 font-weight-bold mb-3"><%= form.label :name%></div>
        <div class="form-group d-flex align-items-center">
          <%= form.text_field :name, class: "form-control" %>
        </div>    
          <%= form.submit (t "create_singer"), class: "btn btn-primary mb-3"%>                    
    </form>
  </div>
<% end %>
